Config = {}

Config.IsDriftItemNeededForRace = false      -- If true, the player will need to have the drift item to start the race, if false, the player can start the drift race in all case 
                                            -- Set to false if you don't plan to make the drift item available to players
Config.StatsLimit = 10                      -- Limit to Stats call in the DB
Config.WhitelistedCreators = {
    "DAX96410",
}

Config.Tracks = {}